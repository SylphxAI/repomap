# Predictable defaults

`mode: full` builds a complete local graph, `status_only` checks freshness, and
`auto` is retained only as an incremental-refresh compatibility alias. No remote
provider or model is required.
