# Spine — brand-sole publish

| Field | Value |
| --- | --- |
| Canonical npm | `@sylphx/spine` (packages/mcp-server) |
| bin | `spine` |
| MCP | `io.github.SylphxAI/spine` |

```bash
npm i -g @sylphx/spine
# or
npx @sylphx/spine
```

Native optionalDependencies use `@sylphx/spine-<platform>` and match the product
version.
