---
"@sylphx/spine": patch
---

The published manifest gains real `keywords` (code analysis, repository analysis, impact analysis, call graph, AST, agent context, and the family terms), so npm search can surface the package for the queries its users type.
