---
"@sylphx/spine": patch
---

Symbol evidence uses the line the match is on, including a match at column 0. Tool envelopes and the MCP server version follow `packages/mcp-server/package.json` instead of the private root package or a stale fallback.
