name: Publish brand alias

# RETIRED (brand-sole hard cut): @sylphx/spine is the sole public MCP package id.
# Dual-publish from transitional package ids is no longer valid.

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type RETIRED (workflow intentionally fails closed)'
        required: true
        type: string

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Brand-sole — dual alias path retired
        run: |
          set -euo pipefail
          echo "::error::publish-brand-alias is retired. Canonical package is @sylphx/spine."
          exit 1
