name: Release

on:
  push:
    branches:
      - main
    # A version bump touches packages/mcp-server/package.json, so it still
    # releases. Docs, the README, and a lockfile refresh after natives exist do not.
    paths-ignore:
      - 'docs/**'
      - 'README.md'
      - 'bun.lock'
  workflow_dispatch:

concurrency: ${{ github.workflow }}-${{ github.ref }}

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.4.0'

      - name: Setup Node (changesets publish requires npm on PATH)
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Rust engine
        run: rm -rf target && bun run build:rust

      - name: Run tests
        run: cargo test -p architecture-reader-core -- --test-threads=1 && bun test packages/mcp-server/test/boundary.test.ts packages/mcp-server/test/synth.boundary.test.ts

      - name: Release gate
        run: bun run benchmark:release-gate

      - name: Configure npm auth for changesets publish
        run: |
          set -euo pipefail
          test -n "${{ secrets.NPM_TOKEN }}"
          printf '//registry.npmjs.org/:_authToken=%s\nalways-auth=true\n' "${{ secrets.NPM_TOKEN }}" > "$HOME/.npmrc"

      - name: Create release PR or publish
        uses: changesets/action@v1
        with:
          version: bun run version-packages
          publish: bun run release
          title: 'chore(release): version packages'
          commit: 'chore(release): version packages'
          createGithubReleases: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
